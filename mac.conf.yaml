- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.gitconfig: gitconfig
    ~/.dotfiles: ''

- create:
    - ~/.config
    - ~/.ssh

- brew:
    - git
