- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc
    ~/bin: bin
    ~/.gitconfig: gitconfig

- create:
  -  ~/.ssh

- shell:
  - [git submodule update --init --recursive, Installing submodules]
