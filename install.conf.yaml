- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.tmux.conf: tmux.conf
    ~/.tmux: tmux/
    ~/.zshrc: zshrc
    ~/bin: bin

- create:
  -  ~/.ssh

- shell:
  - [git submodule update --init --recursive, Installing submodules]
